apiVersion: v1
kind: Service
metadata:
  name: {{ include "numbers-api.fullname" . }}
  labels:
    {{- include "numbers-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "numbers-api.selectorLabels" . | nindent 4 }}
