apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "numbers-api.fullname" . }}-test-connection"
  labels:
    {{- include "numbers-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "numbers-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
