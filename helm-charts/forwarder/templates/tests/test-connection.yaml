apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "forwarder.fullname" . }}-test-connection"
  labels:
    {{- include "forwarder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "forwarder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
